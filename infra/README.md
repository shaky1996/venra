# Infrastructure

This directory is reserved for Venra's infrastructure configuration.

The first planned addition is a simple Docker Compose setup for running PostgreSQL locally. It will be added when the backend reaches the persistence phase so its configuration matches a real application requirement.

There is currently no Docker, PostgreSQL, AWS, deployment, authentication, AI, or OCR configuration in this directory.
